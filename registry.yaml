registries:
  create: #same as `--registry-create demo-registry`
    name: my-registry # default image docker.io/library/registry:2
    host: "0.0.0.0"
    hostPort: 6500

 
##  k3d registry create my-registry